<div class="contents row">
  
  <%= form_for [@notice] do |f| %>        
            <div style="margin: 8px 0">
                <%= f.label :タイトル, style: { 'margin-right' => 8 } %>
                <%= f.text_field :title, placeholder: 'title'%>
            </div>
        
            <div style="margin: 8px 0">
                <%= f.label :気づいたこと, style: { 'margin-right' => 8 } %>
                <%= f.text_area :notice, placeholder: 'notice', style: 'width: 90%; height: 300px;' %>
            </div>
            
            <% if @notice.image.attached? %>
                <div><%= image_tag @notice.image.variant(resize:'280x280') %></div>
            <% end %>
            
            <div class="field">
                <%= f.file_field :image %>
            </div>
        
            <div class="row">
                <div class="col10 push1">
                  <%= button_tag type: "submit", class: "btn btn--block" do %>
                  更新する<i class="icon-arrow-right"></i>
                  <% end %>
                </div>
            </div>        
    <% end %>
</div>